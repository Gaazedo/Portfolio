from utility import*

def iguais1(vetor1, vetor2):
    m1 = len(vetor)
    m2=len(vetor2)
    iguais =" "
    for i in range(m1):
            iguais = iguais + str(vetor[i]) + " "
    for j in range(m2):
      iguais = iguais + str(vetor2[j]) + " "

    return iguais
